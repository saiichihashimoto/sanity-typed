# https://github.com/repository-settings/app
_extends: .github
repository:
  name: sanity-typed
  description: End to end Sanity types without any explicit typing, from config to fetch!
  private: false
branches:
  - name: alpha
    protection:
      required_status_checks:
        contexts:
          - Build
          - Lint
          - Test
  - name: main
    protection:
      required_status_checks:
        contexts:
          - Build
          - Lint
          - Test
