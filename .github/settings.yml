# https://github.com/repository-settings/app
_extends: .github
repository:
  name: sanity-typed
  description: Type Sanity from config to fetch with zero schema changes!
  private: false
branches:
  - name: alpha
    protection:
      required_status_checks:
        contexts:
          - Build
          - Lint
          - Test
  - name: main
    protection:
      required_status_checks:
        contexts:
          - Build
          - Lint
          - Test
